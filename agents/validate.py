def validate(value, checks, config):
    """
    Returns: {
        "passed": bool,
        "reason": "Email format valid, not null",
        "failed_checks": [],
        "value": "test@example.com"
    }
    """
